# React Todo — Starter

A minimal Vite + React + TypeScript starter for the TechLearn "Build a Todo App with React" project.

Open `src/App.tsx` and complete the three `TODO` tasks (add, toggle, delete).

## Run locally

```bash
npm install
npm run dev
```

In the in-browser IDE the preview updates as you type — no setup needed.
